/**
 * RPC type surface for the frontend `hc` client.
 *
 * `apps/web` imports `AppType` from here (type-only) to get end-to-end typed API
 * calls without bundling any server code.
 */
export type { AppType } from './index'
