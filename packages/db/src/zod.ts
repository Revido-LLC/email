/**
 * Zod request/response schemas — the shared validation contract (W1/W2).
 *
 * Derived from the Drizzle schema via `drizzle-zod` plus hand-written API
 * request/response shapes that back `docs/api-contract.md`. Consumed by `apps/api`
 * (route validation) and re-used by `apps/web` hooks for end-to-end type safety.
 *
 * Filled in by the Wave 1 `db-schema` agent. This stub keeps `@revido/db/zod`
 * resolvable.
 */
export {}
